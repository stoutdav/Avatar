# Avatar configuration file

# Configure the command producer
producer.class = com.avatarbot.avatar.mock.MockCommandProducer

# Configure the command consumer
consumer.class = com.avatarbot.avatar.mock.MockCommandConsumer
